# ==============================================================================
# Next.js Environment Variables
# @see https://nextjs.org/docs/app/guides/environment-variables
#
# To expose environment variables to the browser, they must be prefixed with 'NEXT_PUBLIC_'.
# Variables without the prefix are only available on the server-side.
# ==============================================================================

# ------------------------------------------------------------------------------
# Mock Service Worker (MSW) Configuration
# ------------------------------------------------------------------------------
# The port on which the mock server will run.
# This is used for server-side mocking in development.
MOCK_PORT=9090

# ------------------------------------------------------------------------------
# API Configuration
# ------------------------------------------------------------------------------
# The base URL for the API. This is exposed to the browser.
NEXT_PUBLIC_API_URL=http://localhost:9090

# ------------------------------------------------------------------------------
# Deployment Environment
# ------------------------------------------------------------------------------
# The current deployment environment (e.g., 'development', 'production').
# Defaults to the value of NODE_ENV. This is exposed to the browser.
NEXT_PUBLIC_DEPLOY_ENV=$NODE_ENV

# ------------------------------------------------------------------------------
# Sentry (Error Tracking & Performance Monitoring)
# @see https://docs.sentry.io/
# ------------------------------------------------------------------------------
# The Data Source Name (DSN) for your Sentry project. This is required for the
# browser-side client to capture and report errors.
NEXT_PUBLIC_SENTRY_DSN=''

# The authentication token for the Sentry API.
# This is used by the Sentry SDK during the build process to upload
# source maps, which helps in debugging minified production code.
# This value is NOT exposed to the browser.
SENTRY_AUTH_TOKEN=''



